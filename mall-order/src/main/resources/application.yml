server:
  port: 9000

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.31.117:3306/gulimall_oms
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml  #classpath*:/  引用得依赖classpath都寻找

  global-config:
    db-config:
      id-type: auto    #auto increase

